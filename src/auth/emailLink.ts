// Where we stash the pending sign-in email between sending the magic link
// and the user opening the link (possibly on a different device).
export const EMAIL_LS_KEY = 'pointless:pending-signin-email';
